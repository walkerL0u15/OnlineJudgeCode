#include<stdio.h>
#include<string>
using namespace std;

string a,b;
int as,bs,ap;
void div(){
	
}

int main()
{
	ios::sync_with_stdio(false);cin.tie(0);
	string a,b,ans="";
	int as,bs,ap;
	cin>>a>>b;
	as=a.size()-1,bs=b.size()-1;
	div();
	printf("%s",ans);
	return 0;
}
